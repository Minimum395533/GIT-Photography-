# Photography Gallery

This is a static HTML/CSS/JavaScript photography gallery. The site is served directly from the project root; no dependency installation or secrets are required.

## Run on Replit

Use the **Start application** workflow. It runs:

`python3 -m http.server 5000 --bind 0.0.0.0`

The Replit web preview opens the site on port 5000.